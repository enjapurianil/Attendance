import { SearchAttendancePipe } from './search-attendance.pipe';

describe('SearchAttendancePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAttendancePipe();
    expect(pipe).toBeTruthy();
  });
});
